(* Iterative Bisection *)

iterative_bisection(f, endpoint1, endpoint2, tolerance, max_iterations) :
	new n = 1 
			n := n + 1;
			if(sign(f(c)) = sign(f(a))) {
			 	endpoint1 := c
			} else {
				endpoint2 := c
			}
		}
	}